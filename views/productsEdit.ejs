<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="/style/homePage.css">
<link rel="stylesheet" href="/style/products.css">

    <title>Edit Product</title>
</head>
<body>

    <%- include('partials/header') %>


    <% if (user && user.role === 'admin') { %>
        <h1>Edit Product</h1>
        <form action="/admin/products/edit/<%= collectionName %>/<%= product._id %>" method="POST">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= product.title %>" required>
            <br>
            <label for="img">Image URL:</label>
            <input type="text" id="img" name="img" value="<%= product.img %>" required>
            <br>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= product.name %>" required>
            <br>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" step="0.01" value="<%= product.price %>" required>
            <br>
            <label for="category">Category:</label>
            <select id="category" name="category" required>
                <!-- <option value="בשר">בשר</option>
                <option value="דגים">דגים</option>
                <option value="מוצרי חלב">מוצרי חלב</option>
                <option value="פירות">פירות</option>
                <option value="ירקות">ירקות</option>
                <option value="ניקיון">ניקיון</option>
                <option value="יבשים">יבשים</option>
                <option value="ממתקים וחטיפים">ממתקים וחטיפים</option>
                <option value="משקאות">משקאות</option>
                <option value="קפואים">קפואים</option>
                <option value="לחמים ומאפים">לחמים ומאפים</option> -->
                <% Object.keys(categoryMap).forEach(key => { %>
                    <option value="<%= key %>" <%= product.category === key ? 'selected' : '' %>><%= categoryMap[key] %></option>
                <% }) %>
            </select>
            <br>
            <br>
            <label for="supplier">Supplier:</label>
            <input type="text" id="supplier" name="supplier" value="<%= product.supplier %>" required>
            <br>
            <label for="amount">Amount:</label>
            <input type="number" id="amount" name="amount" value="<%= product.amount %>" required>
            <br>
            <label for="sub">Sub:</label>
            <input type="text" id="sub" name="sub" value="<%= product.sub %>" required>
            <br>
            <br>
            <label for="sub">recommended:</label>
            <select id="recommended" name="recommended" class="form-control" required>
                <option value="כן" <%= product.recommended ? 'selected' : '' %>>כן</option>
                <option value="לא" <%= !product.recommended ? 'selected' : '' %>>לא</option>
            </select>
            <br>
            <button type="submit">Update Product</button>
        </form>
        <a href="/<%= collectionName %>">Back to Product List</a>
    <% } else { %>
        <div>
            <p>You do not have permission to view this page.</p>
            <a href="/">Back to Home</a>
        </div>
    <% } %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
