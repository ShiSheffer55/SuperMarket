<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>cart </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="style/header.css">
    <link rel="stylesheet" href="style/cart.css">

</head>
<body>
    <%- include('partials/header') %>
    <h1 id="title">&#9734;     סל הקניות שלך  &#9734;</h1> 
    <% if (cart.length === 0) { %>
        <p class="empty-cart">סל הקניות ריק</p>
    <% } else { %>
        <p id="numOfProducts" ><%= cart.length%> מוצרים</p>
        <table>
            <thead>
                <tr>
                    <th id="th1">מוצר</th>
                    <th id="th1">מחיר</th>
                    <th id="th1">כמות</th>
                    <th id="th1">סה"כ</th>
                    <th id="th1">מחיקה</th>

                </tr>
            </thead>
            <tbody>
                <% cart.forEach(item => { %>
                    <tr data-product-name="<%= item.title %>"> <!-- Updated to use title -->
                        <td><%= item.title %></td>
                        <td><%= item.price %>₪</td>
                        <td><%= item.quantity %></td>
                        <td><%= (item.price * item.quantity).toFixed(2) %>₪</td>

                        <td>
                            <button class="delete-btn"><i class="fas fa-trash">מחיקה</i></button>
                        </td>
                    </tr>
                <% }) %>
                
                <tr>
                    <td colspan="3" id="th1"><strong></strong></td>
                    <td id="th1"><%= total.toFixed(2) %>₪</td>
                    <td></td>
                </tr>
            </tbody>
        </table>

        <div class="actions">
            <a id="but1"href="#" id="cancel-cart" id="th1">מחק סל</button>
            <a id="but2" href="/cart/checkout" id="th1">המשך לסיום קנייה</a>
            <a id="but3" href="/" id="th1">המשך קנייה</a>
        </div>
    <% } %>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/JavaScript/cart.js"></script>
    <script src="/JavaScript/categoryBar.js"></script>
</body>
</html> 


